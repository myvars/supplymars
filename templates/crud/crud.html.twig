{% if template is defined %}
   {% if section is defined %}
       {{ include([section|lower|u.snake~'/'~template~'.html.twig', 'crud/'~template~'.html.twig' ]) }}
   {% else %}
       {{ include([template, 'crud/'~template~'.html.twig', 'common/missingTemplate.html.twig']) }}
   {% endif %}
{% else %}
    {{ include(['common/missingTemplate.html.twig']) }}
{% endif %}