{% extends 'base.html.twig' %}

{% block title %}Subcategory{% endblock %}

{% block body %}

    <div class="p-3 space-y-3 text-sm text-gray-500 dark:text-gray-400">
        <h1 class="mb-3 text-xl text-gray-900 dark:text-white">Subcategory</h1>
        <div class="flex flex-wrap justify-center gap-3">
            {{ include('subcategory/_subcategory_card.html.twig', {subcategory: result}) }}
        </div>
        <p>
            <a href="{{ path('app_subcategory_index') }}"
               class="font-light hover:text-gray-300 dark:hover:text-white"
               data-turbo-frame="_top"
               data-turbo-prefetch="false"
            >Back to list</a>
        </p>
    </div>

{% endblock %}