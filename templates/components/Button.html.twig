<{{ tag }}{{ attributes.defaults({
    class: 'flex items-center justify-center font-medium rounded-lg text-sm py-1.5 text-center '~this.variantClasses,
}) }}>
    {% block content %}{% endblock %}
</{{ tag }}>
