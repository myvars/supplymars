{% extends 'base.html.twig' %}

{% block title %}Edit Category{% endblock %}

{% block body %}
    <section class="bg-gray-50 dark:bg-gray-900 p-3 sm:p-5">
        <div class="flex flex-col items-center px-6 py-8 mx-auto lg:py-0">
            <div
                class="w-full bg-white rounded-lg shadow dark:border md:m-10 sm:max-w-md xl:p-0 dark:bg-gray-800 dark:border-gray-700">
                <div class="p-6 space-y-4 md:space-y-6 sm:p-8">
                    <h1 class="text-xl font-bold leading-tight tracking-tight text-gray-900 md:text-2xl dark:text-white">
                        Edit Category
                    </h1>
                    {{ include('category/_form.html.twig', {'button_label': 'Update'}) }}
                    <hr class="h-px my-8 bg-gray-200 border-0 dark:bg-gray-700">
                    <twig:Button
                        tag="a"
                        href="{{ path('app_category_delete_confirm', {'id': category.id}) }}"
                        type="submit"
                        variant="danger"
                        class="w-full">
                        Delete
                    </twig:Button>
                    <p class="text-sm font-light text-gray-500 dark:text-gray-400">
                        <a href="{{ path('app_category_index') }}" class="hover:underline">Back to list</a>
                    </p>
                </div>
            </div>
        </div>
    </section>
{% endblock %}