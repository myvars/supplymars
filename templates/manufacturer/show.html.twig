{% extends 'base.html.twig' %}

{% block title %}Manufacturer{% endblock %}

{% block body %}

    <div class="p-4 space-y-4 md:space-y-6 text-sm text-gray-500 dark:text-gray-400">
        <h1 class="mb-4 text-2xl font-semibold text-white">Manufacturer</h1>
        <div class="flex flex-wrap justify-center gap-8">
            {{ include('manufacturer/_manufacturer_card.html.twig', {manufacturer: result}) }}
        </div>
        <div class="mt-4">
            <p>
                <a href="{{ path('app_manufacturer_index') }}"
                   class="font-light hover:text-gray-300 dark:hover:text-white"
                   data-turbo-frame="_top"
                   data-turbo-prefetch="false"
                >Back to list</a>
            </p>
        </div>
    </div>

{% endblock %}