{% extends 'base.html.twig' %}

{% block title %}Manufacturer Details{% endblock %}

{% block body %}

    <div class="m-3 space-y-3">
        {{ include('manufacturer/_manufacturer_card.html.twig', {manufacturer: result}) }}
        {% if result.products.count > 0 %}
            {{ include('manufacturer/_manufacturer_products_card.html.twig', {manufacturer: result}) }}
        {% endif %}
        {{ include('manufacturer/_manufacturer_product_sales_card.html.twig', {category: result}) }}
    </div>

{% endblock %}