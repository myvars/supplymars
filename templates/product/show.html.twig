{% extends 'product/_navigation.html.twig' %}

{% block title %}Product Details{% endblock %}

{% block detail %}

    <div class="p-6 sm:p-8 space-y-4 md:space-y-6 text-sm text-gray-500 dark:text-gray-400">
        <h1 class="mb-4 text-2xl font-semibold text-white">Product Details</h1>
        <div class="flex flex-wrap justify-center gap-8">
            {{ include('product/_product_card.html.twig', {product: result}) }}
        </div>
        <div class="mt-4">
            <p>
                <a href="{{ path('app_product_index') }}"
                   class="font-light hover:text-gray-300 dark:hover:text-white"
                   data-turbo-frame="_top"
                   data-turbo-prefetch="false"
                >Back to list</a>
            </p>
        </div>
    </div>

{% endblock %}