<div class="grid grid-cols-2 gap-px overflow-hidden rounded-lg bg-gray-900/5 dark:bg-white/5">
    <twig:KpiCard title="Total Items" value="{{ summary.itemCount|default(0) }}"/>
    <twig:KpiCard title="Total Profit" value="{{ summary.totalProfit|default(0) }}" valueType="currency"/>
</div>
