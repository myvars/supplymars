{# Ticket detail page — composes _ticket_card, conversation feed, and embedded reply form.
   Variable: result (Ticket), replyForm (?FormView) #}
{% extends 'base.html.twig' %}

{% block title %}Ticket: {{ result.subject }}{% endblock %}

{% block body %}

    <div class="m-3 space-y-4">
        <twig:Breadcrumb :href="path('app_note_ticket_index')" label="Tickets" :current="result.subject"/>
        {{ include('note/ticket/_ticket_card.html.twig', {ticket: result}) }}

        <twig:Card>
            <div class="mb-2 flex items-center gap-2">
                <p class="text-xs font-medium uppercase tracking-wide text-gray-500 dark:text-gray-500">Conversation</p>
                <span class="ms-auto inline-flex h-5 min-w-5 items-center justify-center rounded-full bg-gray-200 px-1.5 text-xs font-medium text-gray-700 dark:bg-gray-500 dark:text-gray-200">
                    {{ result.messages|length }}
                </span>
            </div>
            {% if result.messages is empty %}
                <twig:EmptyState message="No messages yet"/>
            {% else %}
                <ol class="relative ms-4 border-s-2 border-gray-200 dark:border-white/[0.06]">
                    {% for message in result.messages %}
                        {% if message.isSystem %}
                            {{ include('note/ticket/_feed_event.html.twig', {message: message}) }}
                        {% else %}
                            {{ include('note/ticket/_feed_comment.html.twig', {message: message, is_original: loop.first}) }}
                        {% endif %}
                    {% endfor %}
                </ol>
            {% endif %}
            {% if replyForm %}
                <div class="mt-4 border-t border-gray-200 pt-4 dark:border-white/[0.06]">
                    <turbo-frame id="ticket-reply">
                        <p class="mb-2 text-xs font-medium uppercase tracking-wide text-gray-500 dark:text-gray-500">Reply</p>
                        <twig:FlowForm :form="replyForm" buttonLabel="Send Reply" formFrame="ticket-reply"/>
                    </turbo-frame>
                </div>
            {% endif %}
        </twig:Card>

    </div>

{% endblock %}
