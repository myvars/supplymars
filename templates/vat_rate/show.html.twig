{% extends 'base.html.twig' %}

{% block title %}VAT Rate{% endblock %}

{% block body %}

    <div class="p-6 sm:p-8 space-y-4 md:space-y-6 text-sm text-gray-500 dark:text-gray-400">
        <h1 class="mb-4 text-2xl font-semibold text-white">VAT Rate</h1>
        <div class="flex flex-wrap justify-center gap-8">
            {{ include('vat_rate/_vat_rate_card.html.twig', {vatRate: result}) }}
        </div>
        <div class="mt-4">
            <p>
                <a href="{{ path('app_vat_rate_index') }}"
                   class="font-light hover:text-gray-300 dark:hover:text-white"
                   data-turbo-frame="_top"
                   data-turbo-prefetch="false"
                >Back to list</a>
            </p>
        </div>
    </div>

{% endblock %}



