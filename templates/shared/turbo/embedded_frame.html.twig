{% set _turbo_frame = app.request.headers.get('turbo-frame') %}
<turbo-frame id="{{ _turbo_frame }}">
    {% block body %}{% endblock %}
    {{ include('shared/turbo/_frame_success_stream.html.twig', { frame: _turbo_frame }) }}
</turbo-frame>
