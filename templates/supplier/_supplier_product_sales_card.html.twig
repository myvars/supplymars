<twig:Card colour="{{ supplier.colourScheme }}">
    <turbo-frame
        id="sales"
        src="{{ path('app_product_sales_dashboard', { supplier_id: result.id }) }}"
        refresh="morph"
    ></turbo-frame>
</twig:Card>
