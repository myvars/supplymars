{% for label, messages in app.flashes(['success','info','warning','danger']) %}
    {% for message in messages %}
        <twig:Toast type="{{ label }}">{{ message }}</twig:Toast>
    {% endfor %}
{% endfor %}
